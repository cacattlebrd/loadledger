{
  "version": "2.6.8",
  "min_version": "2.6.8",
  "released": "2026-05-14",
  "forceUpdate": true,
  "notes": "Fixes duplicate row bug on direct SUBMIT LOAD path. Button now disables on tap and re-enables when backend confirms (success or fail). Prevents double-tap from creating two RAW Loads rows with two different timestamps and Row IDs. The Save-for-Later (START + FINISH) flow was already safe via the loadId lookup. No backend change needed."
}