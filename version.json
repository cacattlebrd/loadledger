{
  "version": "2.6.17",
  "min_version": "2.6.17",
  "released": "2026-05-18",
  "forceUpdate": true,
  "notes": "History log fixes - Load Submitted entries now show the actual customer or supplier name (was showing ? because the code was checking the wrong field). Load Finished entries now show direction + customer/supplier instead of the LD-XXXX internal ID, which nobody uses anyway."
}
