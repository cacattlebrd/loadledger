{
  "version": "2.6.7",
  "min_version": "2.6.7",
  "released": "2026-05-13",
  "forceUpdate": true,
  "notes": "Admin config sync (customers/suppliers/etc) now verifies the backend actually accepted the save. Previously the phone said \"synced\" even when the backend silently dropped the payload, leaving other devices on stale lists. Requires backend Apps Script patch to take effect end-to-end."
}